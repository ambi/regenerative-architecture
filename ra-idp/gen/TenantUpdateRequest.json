{
  "$id": "urn:idp:TenantUpdateRequest",
  "type": "object",
  "additionalProperties": false,
  "description": "管理者がテナントの表示名を更新する JSON リクエスト (ADR-032)。`id` と status は変更しない。status 変更は disable/enable 専用 endpoint を経由。",
  "properties": {
    "display_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    }
  }
}
