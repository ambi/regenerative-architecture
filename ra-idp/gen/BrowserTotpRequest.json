{
  "$id": "urn:idp:BrowserTotpRequest",
  "type": "object",
  "additionalProperties": false,
  "description": "SPA が POST /api/auth/totp に送る TOTP コード。authorization request id は HttpOnly transaction cookie、CSRF は X-CSRF-Token header から解決する。他 factor (WebAuthn 等) は HTTP 形が大きく異なるため、別 endpoint と別 value object として独立に定義する。",
  "properties": {
    "code": {
      "type": "string"
    }
  },
  "required": [
    "code"
  ]
}
