{
  "$id": "urn:idp:ParResponse",
  "type": "object",
  "additionalProperties": false,
  "description": "/par レスポンス (RFC 9126 §2.2)。短命の request_uri を発行し、後続の /authorize から参照される。",
  "properties": {
    "request_uri": {
      "type": "string"
    },
    "expires_in": {
      "type": "integer"
    }
  },
  "required": [
    "request_uri",
    "expires_in"
  ]
}
